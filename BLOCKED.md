# 待裁决清单

- 任务 3：项目既有 `script/build_and_run.sh` 在每次启动前执行 `rm -rf dist/CodexQ.app` 等删除操作；任务书禁止任何删数据，故不运行该脚本。启动验证改为直接运行已构建的 SwiftPM 可执行文件，仅测进程存活；这不能证明 bundle 重建路径，但避免越过删除禁令。

- 任务 0 基线与 2026-07-27 任务书不一致：`~/.codex/sessions` 当前为 71 个 `.jsonl`、127,265,025 字节（`du -sh` 为 122M），任务书为 69 个、121MB。真实数据集已变化，任务 2 的绝对耗时不能与任务书中的历史数据直接比较；后续将先在当前快照上记录优化前后可比基准。release `dist/` 当前为 4.2M，任务书为 3.9MB；可执行文件仍为 2,342,176 字节，满足冻结上限。
